{this.state.albumDetail.forEach((el) => {
                if (el.id == album.id) {
                  console.log(el.albumId);
                  return (
                    <>
                      <div className="card-img">{el.url}</div>
                    </>
                  );
                }
              })}